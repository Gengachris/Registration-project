version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: student_registration_db
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: studentauthdb
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
